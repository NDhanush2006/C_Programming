// Single line comment -Single-line comments start with two forward slashes (//).Any information after the slashes // lying on the same line would be ignored (willnot be executed).

/* Multiline comment :- A multi-line comment starts with /* and ends with /. Any
information between /* and  will be ignored by the compiler.
Note: Comments in a C program are not executed and are ignored */

#include<stdio.h>

int main(){
    // This is First program
    printf("Hello world!");

    return 0;
}