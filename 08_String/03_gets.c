#include<stdio.h>

int main(){
    char st[30];
    //gets() - is a function which can be used to receive multi word string.
    gets(st);
    // printf("%s",st);
    puts("Hello");
    // printf("%s",st);


    return 0;
}